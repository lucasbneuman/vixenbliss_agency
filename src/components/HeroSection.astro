<section id="inicio" class="hero-section" data-hero-section aria-labelledby="hero-title">
  <div class="hero-section__inner">
    <p class="hero-section__brand">VixenBliss Agency</p>
    <h1 id="hero-title">Experiencias visuales impulsadas por IA</h1>
    <p class="hero-section__subtitle">
      Explorá una nueva generación de contenido digital y avatares creativos
    </p>
  </div>
</section>

<style>
  .hero-section {
    display: grid;
    min-height: 100vh;
    min-height: 100svh;
    place-items: center;
    scroll-snap-align: start;
    scroll-snap-stop: always;
    overflow: hidden;
    padding: 6.5rem 1.15rem 3rem;
    background:
      radial-gradient(circle at 50% 26%, rgba(255, 95, 135, 0.16), transparent 34rem),
      linear-gradient(180deg, #101017 0%, #07070b 58%, #050507 100%);
  }

  .hero-section__inner {
    display: grid;
    justify-items: center;
    gap: 1rem;
    width: min(100%, 52rem);
    text-align: center;
  }

  .hero-section__brand,
  h1,
  .hero-section__subtitle {
    margin: 0;
  }

  .hero-section__brand {
    color: rgba(255, 255, 255, 0.66);
    font-size: 0.76rem;
    font-weight: 800;
    letter-spacing: 0.16em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  h1 {
    max-width: 10ch;
    color: #fff;
    font-size: clamp(3.1rem, 15vw, 7.2rem);
    font-weight: 850;
    line-height: 0.92;
    text-wrap: balance;
  }

  .hero-section__subtitle {
    max-width: 34rem;
    color: rgba(255, 255, 255, 0.74);
    font-size: clamp(1rem, 4vw, 1.25rem);
    line-height: 1.55;
    text-wrap: balance;
  }

  @media (min-width: 768px) {
    .hero-section {
      padding: 7rem 2rem 3.5rem;
    }

    h1 {
      max-width: 12ch;
    }
  }
</style>
